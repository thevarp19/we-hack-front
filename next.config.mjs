/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ["img.freepik.com"],
    },
    reactStrictMode: false,
};

export default nextConfig;
